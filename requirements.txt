# Core dependencies
chromadb
ollama
duckduckgo-search
plyer
diff-match-patch
wikipedia-api
fastapi
uvicorn
python-dotenv
structlog

# Optional but recommended
psutil
keyring
cryptography

# For future graph features
networkx
graspologic

# Development / Testing
pytest
ruff

# PyTorch (CPU version - recommended for stability on Windows)
torch
torchvision
torchaudio
--index-url https://download.pytorch.org/whl/cpu

# If you have NVIDIA GPU and want CUDA (faster), use this instead:
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121